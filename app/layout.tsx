import type { Metadata } from "next";
import { Inter, Space_Grotesk } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "LedgerFlow Pro — Your Financial Operating System",
  description:
    "Premium fintech dashboard for managing transactions, invoices, reports, and analytics with AI-powered insights.",
  keywords: ["fintech", "dashboard", "accounting", "ledger", "analytics"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${inter.variable} ${spaceGrotesk.variable} font-sans antialiased bg-bg-main text-text-primary`}
      >
        {children}
      </body>
    </html>
  );
}
